# Test Baseline — Status Report

> Generated by Agent 0 on 2026-07-13.
> **NO tests have been executed in this reconciliation phase.** All prior "passing" claims are UNVERIFIED.

## 1. Prior Test Claims (UNVERIFIED)

The memory context references claims such as "271 tests passing" and "gate.mjs exit code 0". These claims came from prior Agent reports and execution sessions.

**Search result**: No occurrence of "271 tests" or "271 passing" exists in any file in the repository. The claim was only in session memory, not in committed documentation. Per reconciliation rules, these claims are treated as **REPORT_ONLY** until independently verified.

## 2. Discovered Test Files (current main, c4ae36e)

### TypeScript Unit Tests (18 files, in `mw-backend/src/`)
| # | File | Category |
|---|------|----------|
| 1 | `src/routes/admin-review-integration.test.ts` | route/inject |
| 2 | `src/plugins/auth-role.test.ts` | unit (mock) |
| 3 | `src/plugins/readiness.test.ts` | unit (mock) |
| 4 | `src/plugins/bigint-serialization.test.ts` | unit |
| 5 | `src/security/ssrf.test.ts` | unit |
| 6 | `src/security/ssrf-redirect.test.ts` | unit |
| 7 | `src/security/redisGuard.test.ts` | unit (mock) |
| 8 | `src/domain/review/repository.test.ts` | unit (mock) |
| 9 | `src/domain/review/fingerprint.test.ts` | unit |
| 10 | `src/review/repository.test.ts` | unit (mock) |
| 11 | `src/review/service.test.ts` | unit (mock) |
| 12 | `src/crawler/stateMachine.test.ts` | unit |

### TypeScript Integration Tests (6 files, in `mw-backend/tests/`)
| # | File | Category |
|---|------|----------|
| 13 | `tests/review-action-audit.test.ts` | integration (mock) |
| 14 | `tests/reopen.test.ts` | integration (mock) |
| 15 | `tests/real-image-count.test.ts` | integration (mock) |
| 16 | `tests/duplicate-suppression.test.ts` | integration (mock) |
| 17 | `tests/crawler-transition.test.ts` | integration (mock) |
| 18 | `tests/bigint-serialization.test.ts` | unit |

### JavaScript Tests (1 file)
| # | File | Category |
|---|------|----------|
| 19 | `modelwiki-theme/tests/admin-ui-check.mjs` | static analysis (PHP JS syntax check) |

### Python Tests (2 files)
| # | File | Category |
|---|------|----------|
| 20 | `modelwiki-theme/tests/test_material_parser.py` | unit |
| 21 | `test_crawler_state.py` (root) | unit |

### Total: 21 test files discovered

## 3. Test Execution Status

| Category | Discovered | Executed | Passed | Failed | Skipped |
|----------|-----------|----------|--------|--------|---------|
| TypeScript unit (src/) | 12 | 0 | — | — | — |
| TypeScript integration (tests/) | 6 | 0 | — | — | — |
| JavaScript static | 1 | 0 | — | — | — |
| Python | 2 | 0 | — | — | — |
| PostgreSQL integration | 0 | 0 | — | — | — |
| Redis integration | 0 | 0 | — | — | — |
| Migration | 0 | 0 | — | — | — |
| PHP syntax (`php -l`) | 0 | 0 | — | — | — |
| Browser E2E | 0 | 0 | — | — | — |
| **Total** | **21** | **0** | **—** | **—** | **—** |

**NO tests have been executed in this reconciliation.** All counts are TBD pending clean-checkout verification (Agent D's task).

## 4. Test Configuration

From `mw-backend/package.json`:
- `npm test` → `tsx --test "src/**/*.test.ts"` (unit tests only)
- `npm run test:integration` → `tsx --test "tests/**/*.test.ts"` (integration tests)
- `npm run gate` → `node scripts/gate.mjs` (8-gate QA: prisma validate, typecheck, build, unit tests, admin-js-check, PHP syntax, Python tests, secret scan)

## 5. Gate Script (gate.mjs)

`mw-backend/scripts/gate.mjs` runs 8 gates:
1. `prisma validate`
2. `tsc --noEmit` (typecheck)
3. `tsup` (build)
4. `tsx --test "src/**/*.test.ts"` (backend unit tests)
5. `node scripts/admin-js-check.mjs` (admin JS syntax)
6. `php -l` (PHP syntax check)
7. `pytest` (Python tests)
8. Secret scan

**Status**: NOT EXECUTED in this reconciliation. Prior claims of "exit code 0" are UNVERIFIED.

## 6. Known Test Environment Issues

1. **auth.ts references non-existent schema fields** — any test touching register/login/verify-email/me will fail at runtime (Prisma error) unless using mocks that completely bypass Prisma.
2. **`app.prisma as any`** in auth.ts (line 124) — typecheck may pass but runtime will fail.
3. **No disposable PostgreSQL/Redis test infrastructure** — current tests appear to use mocks only (no real DB/Redis integration tests exist in main).
4. **PHP availability unknown** — gate.mjs runs `php -l` but PHP may not be installed on this machine.
5. **Python availability unknown** — gate.mjs runs `pytest` but Python/pytest may not be configured.
6. **No E2E browser tests** — none discovered in the repository.

## 7. Required Test Baseline (Agent D's Task)

Agent D must establish a real test baseline from a clean checkout:
- Record Node, npm/pnpm, Python, PHP, PostgreSQL, Redis versions
- Run `npm install` from frozen lockfile
- Run `prisma generate` + `prisma validate`
- Run `prisma migrate deploy` on empty disposable DB
- Run `tsc --noEmit` (typecheck)
- Run `tsup` (build)
- Run unit tests with real execution (not mock-only claims)
- Run integration tests with disposable PostgreSQL + Redis
- Run `php -l` on all PHP files
- Run `pytest` on Python tests
- Run secret scan
- Output JSON manifest: discovered, executed, passed, failed, skipped, duration

**No "VERIFIED" label for mock-only or skipped tests.** Gate must FAIL on any non-zero exit code.

## 8. Account Isolation E2E (Required)

Agent D must create E2E tests verifying:
- Frontend email user can register + login
- Guanli username admin can login
- User JWT cannot access admin endpoints
- Admin JWT cannot access user endpoints
- These tests do NOT exist yet
