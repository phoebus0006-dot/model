# Branch Classification

> Generated by Agent 0 on 2026-07-13.
> Classification scheme: CANONICAL_CANDIDATE / SALVAGEABLE / OBSOLETE / REPORT_ONLY / UNTRUSTED / DUPLICATE

## Classification Summary

| Branch / State | SHA | Classification | Reason |
|---------------|-----|---------------|--------|
| `main` (local) | `c4ae36e` | **CANONICAL_CANDIDATE** | Most complete review/crawler/security work, clean tree, artifacts removed. BUT User.email missing (inherited from origin/main) and auth.ts references non-existent fields. |
| `origin/main` | `9693533` | **OBSOLETE** | Superseded by local main. Contains email removal + temporary artifacts (diff.txt, patches, tar.gz). |
| `recovery/live-ui-admin` | `476f867` | **SALVAGEABLE** | Has email auth (correct) + apply-service (Lane A-J) NOT in main. But different review models (ReviewEvent vs ReviewDecision) and lacks main's security hardening. |
| `review/phase12-r4` | `0320454` | **DUPLICATE** | Subset of recovery/live-ui-admin (14 of its 20 unique commits). No unique content. |
| `master` | `a4c9d82` | **OBSOLETE** | Phase 12/12R baseline. Fully superseded by both main and recovery lines. |
| `agent/admin-ui` | `5c89799` | **DUPLICATE** | Merged into main via `05c7331`. No unique content. |
| `agent/crawler-state` | `cac42ff` | **DUPLICATE** | Merged into main via `62a7207`. No unique content. |
| `agent/qa-ci` | `46070d3` | **DUPLICATE** | Merged into main via `50947b3`. No unique content. |
| `agent/review-api-integration` | `1ea965b` | **DUPLICATE** | Merged into main via `c4ae36e`. No unique content. |
| `agent/review-storage` | `df45dae` | **DUPLICATE** | Merged into main via `70b81b7`. No unique content. |
| `agent/runtime-security` | `23b952f` | **DUPLICATE** | Merged into main via `52ae58c`. No unique content. |

## Dangling Commit Classification

| SHA | Message | Classification | Reason |
|-----|---------|---------------|--------|
| `a758383` | Add keep_pending patch for admin.ts | **SALVAGEABLE** | Production fix not in any branch. Needs review for relevance. |
| `cc705a9` | Fix 429 root cause: figure count storm + URL cache | **SALVAGEABLE** | Production fix. Has 4 duplicate iterations (d7cafce, 80e5084, 474e77d, f9bede0). |
| `2361475` | Complete candidate cache closure: signed URLs, security, frontend | **SALVAGEABLE** | Production feature not in any branch. |
| `0d9ab33` | Fix candidate proxy auth (fetch+blob), crawler upload edge cases | **SALVAGEABLE** | Production fix. |
| `254219c` | Add admin image proxy with SSRF protection | **SALVAGEABLE** | Production feature. |
| `397a15e` | Increase proxy rate limit 60->600/min, fix lightbox | **SALVAGEABLE** | Production fix. |
| `7c9b28a` | Fix production regressions: 429, UI restore, rate limits | **SALVAGEABLE** | Production fix. |
| `88dbffe` | Fix candidate proxy, XSS, update_job 429, upload failure | **SALVAGEABLE** | Production fix (security: XSS). |
| `99d3ada` | fix: complete phase 12 R4 production safety gates | **SALVAGEABLE** | Production gates. |
| `7ca4cff` | Add review candidate image cache system | **SALVAGEABLE** | Production feature. |
| `69857e4` | Fix material mapping tests + keep_pending with reason prompt | **SALVAGEABLE** | Production fix + tests. |
| `7835686` | Fix keep_pending: save user-entered reason | **SALVAGEABLE** | Production fix. |
| `4d9f77a` | Sync production source files, add material parser tests | **SALVAGEABLE** | Production sync + tests. |
| `edbf659` | Task C: Fix material mapping, Task A: keep_pending records reviewer | **SALVAGEABLE** | Production fix. |
| `ea6c9cf` | Add env to gitignore, exclude m6fix_candidates.json | **SALVAGEABLE** | Config fix. |
| `cbfc298` | feat(review-storage): PG source-of-truth models (pre-amend) | **DUPLICATE** | Pre-amend version of `3699086` (which is in main). |
| `3f69805` | Initial commit: ModelWiki codebase | **DUPLICATE** | Likely orphan/duplicate of `32c1f1f`. |
| Stash WIP snapshots (7) | "WIP on ..." / "index on ..." | **OBSOLETE** | Auto-generated stash snapshots, redundant with branches. |

## Detailed Analysis

### CANONICAL_CANDIDATE: `main` (c4ae36e)

**Strengths:**
- Contains ReviewItem, ReviewDecision, CrawlerJob, CrawlerJobEvent (matches Agent E spec)
- Has DomainReviewRepository, computeCanonicalFingerprint, LEGACY_STATUS_MAP
- Has security hardening: SSRF protection, redisGuard (SCAN-based), readiness endpoint, BigInt serialization
- Has gate.mjs (8-gate QA script), GitHub Actions CI
- Has idempotent migrations (CREATE TABLE IF NOT EXISTS)
- Working tree CLEAN, temporary artifacts already removed
- 28 commits ahead of origin/main, all traceable via reflog

**Weaknesses (MUST be fixed before canonical approval):**
1. **User model has NO email field** — violates product decision. Inherited from origin/main (9693533) where email was removed in commits f1d1dfc→a09b54f→f62fbe7→9693533.
2. **auth.ts is BROKEN** — references `email`, `emailVerifiedAt`, `emailVerifyTokenHash`, `emailVerifyExpiresAt` fields that don't exist in current schema. Only hidden by `app.prisma as any` (line 124). This is a runtime bomb.
3. **No AdminAccount model** — guanli admin uses same User table with role field, violating product decision requiring separate account systems.
4. **No apply-service** — the apply-service implementation (Lane A-J) from recovery/live-ui-admin is NOT present.
5. **Test baseline unverified** — no clean-checkout test run has been performed; prior "passing" claims are from Agent reports, not independently verified.

### SALVAGEABLE: `recovery/live-ui-admin` (476f867)

**Salvageable content:**
1. **User model WITH email** — `email String? @unique`, `emailVerifiedAt`, `emailVerifyTokenHash`, `emailVerifyExpiresAt`. This is the correct schema per product decision.
2. **auth.ts with email login** — `registerSchema` accepts `email + password + displayName`; `loginSchema` accepts `email` or `username` + `password`. Consistent with schema.
3. **apply-service** (10 files in `src/modules/reviews/`):
   - `apply-business.ts`, `apply-dependencies.ts`, `apply-errors.ts`, `apply-handler.ts`
   - `apply-lock.ts`, `apply-route.ts`, `apply-schemas.ts`, `apply-service.ts`, `apply-types.ts`
4. **ReviewApplyAttempt model** — in schema.prisma (not in main)
5. **Test suite** (in `src/test/`):
   - `admin-concurrency.test.ts`, `apply-business.test.ts`, `apply-lock.test.ts`
   - `image-security.test.ts`, `migration-*.test.ts`, `postgres-review-store.test.ts`
   - `review-apply-attempt.integration.test.ts`, `review-contract.test.ts`
6. **Lane-A through Lane-J work**: strict apply schemas, image downloader IP pinning, async locks, admin-user invariant, Phase 13 frontend

**Cannot be canonical baseline because:**
1. **Different review models**: has `ReviewEvent` (not `ReviewDecision`), no `CrawlerJob`/`CrawlerJobEvent`. Does not match Agent E spec.
2. **Lacks main's security hardening**: no SSRF hardening, no redisGuard, no readiness, no BigInt serializer.
3. **Lacks gate.mjs and CI scaffolding**.
4. **Built on older base** (master/a4c9d82), not on current origin/main.
5. **Contains temporary artifacts** (patches, tar.gz — though these may be in .gitignore).

**Recommended salvage approach:**
- Do NOT cherry-pick the entire branch (architectural divergence too large).
- DO use as reference for: email schema restoration, auth.ts email login, apply-service port, ReviewApplyAttempt model.
- Agent A should reference this branch's User model + auth.ts when restoring email.
- Agent E should reference this branch's apply-service when porting (manual port, not cherry-pick, due to structural differences).

### Salvageable Dangling Commits (Production Fixes)

These 15 dangling commits contain production fixes that were never merged into any branch. They represent real production work that would be lost without the backup.

**Recommended handling:**
- Do NOT cherry-pick blindly — many are iterative (e.g., 4 versions of "Fix 429 root cause").
- Review each for unique content during Agent E's review-storage-recovery phase.
- The material mapping fixes (`69857e4`, `edbf659`, `4d9f77a`) and keep_pending fixes (`a758383`, `7835686`) may contain valuable logic for the review workflow.
- The image proxy / SSRF work (`254219c`, `0d9ab33`, `88dbffe`) may complement main's existing SSRF hardening.
- All are preserved in the git bundle and recovery branches for future reference.

## Artifact Classification (files in origin/main but removed in local main)

These files existed in `origin/main` (9693533) but were removed in local `main` (c4ae36e):

| File | Classification | Status in main |
|------|---------------|----------------|
| `diff.txt` | REPORT_ONLY | Removed (2316 lines of diff output) |
| `m6fix_candidates.json` | REPORT_ONLY | Removed (535 lines of candidate JSON) |
| `mw-backend/backend_code.txt` | REPORT_ONLY | Removed (4261 lines of code dump) |
| `mw-backend/src.tar.gz` | UNTRUSTED | Removed (binary source archive) |
| `phase12r4-global-review.patch` | REPORT_ONLY | Removed (2623 lines patch) |
| `phase12r4-global-review.patch.sha256` | REPORT_ONLY | Removed (checksum) |
| `phase12r4-global-source-review.tar.gz` | UNTRUSTED | Removed (993KB binary archive) |
| `phase12r4-global-source-review.tar.gz.sha256` | REPORT_ONLY | Removed (checksum) |

**All above artifacts are correctly removed from local main.** They exist only in origin/main and in the git bundle backup. No unique source code was lost — these are reports/patches/archives, not source files.
